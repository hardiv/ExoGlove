from utils import*

# g = glove.Glove()

# grabbable_objects = ['Red', 'Green', 'Yellow', 'Not a']


if __name__ == "__main__":
    print(isGripNeeded())
    '''
    while True:
        if isGripNeeded():
            g.gripObject(2)
            g.relax()
    '''
